export interface ClientType {

    id?: number,

    name: string,
    email?: string,
    document?: string,
    number?: string,
    info?: string,

    updatedAt?: string,
    createdAt?: string, 
}